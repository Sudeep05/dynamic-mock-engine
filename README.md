# dynamic-mock-engine
Mock Logic Controller (MLC): A high-performance, lightweight Node.js middleware for performance testing. Features real-time route management, CSV-based round-robin data injection, persistence (mocks.json), and request payload logging. Designed for zero-bottleneck fault simulation and dynamic environment mocking via HTTP (3000) and HTTPS (3443).
